<!DOCTYPE html>   
<html>   
  <head>
    <title>Products List</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  </head>
  <body>   
    <div class="container">   
      <h3 class="mt-4">PRODUCTS LIST</h3>   
         
      <div class="mb-4">   
        <%= link_to 'Add Product', new_product_path, class: 'btn btn-primary' %>   
      </div>   
      <br>   
      
      <table class="table table-bordered">   
        <thead class="thead-dark">   
          <tr>   
            <th>Name</th>   
            <th>Price</th>   
            <th>Description</th>  
            <th>Image</th> 
            <th>Actions</th>   
          </tr>   
        </thead>   
        <tbody>   
          <% @products.each do |product| %>   
            <tr>   
              <td><%= product.name %></td>   
              <td><%= product.price %></td>   
              <td><%= truncate(product.short_description, length: 75) %></td>   
            <td> <%= image_tag(product.image, class: 'img-thumbnail', style: 'max-width: 100px; max-height: 100px;') if product.image.present? %> </td>
              <td>   
               <div class="btn-group" role="group">
                <%= link_to 'Show', product_path(product), class: 'btn btn-info btn-sm mr-2' %>   
                <%= link_to 'Edit', edit_product_path(product), class: 'btn btn-warning btn-sm mr-2' %>     
                <%= link_to 'Delete', product_path(product),  data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: 'btn btn-danger btn-sm mr-2' %>

                </div>


              </td>   
            </tr>   
          <% end %>   
        </tbody>   
      </table>   
    </div>   
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
  </body>   
</html>
